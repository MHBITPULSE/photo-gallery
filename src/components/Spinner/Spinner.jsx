import React from 'react'
import './spinner.css'

const Spinner = () => {
      return (
            <div className="loader justify-center items-center">Loading...</div>
      )
}

export default Spinner